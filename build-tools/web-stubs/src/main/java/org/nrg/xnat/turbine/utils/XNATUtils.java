package org.nrg.xnat.turbine.utils;

import org.nrg.xft.security.UserI;

/** Compilation stub for circular dependency resolution. */
@SuppressWarnings("unchecked")
public class XNATUtils {
    /** Returns XnatMrsessiondata at runtime; uses unchecked generic to avoid circular dep. */
    @SuppressWarnings("unchecked")
    public static <T> T getLastSessionForParticipant(String id, UserI user) { return null; }

    public static void setArcProjectPaths(Object arcProject, Object siteConfigPreferences) {}
}
