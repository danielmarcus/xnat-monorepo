/*
 * xnat-api: org.nrg.xdat.model.CatCatalogI
 *
 * Compile-time facade for the codegen interface. Used as the parameter
 * type on getFileStats / formalizeCatalog / Stats constructor in the
 * xnat-api CatalogUtils stub, so descriptors match the apps/web runtime
 * signatures. Empty interface — descriptor match is the only contract.
 * See ADR 0008.
 */
package org.nrg.xdat.model;

public interface CatCatalogI {
}
