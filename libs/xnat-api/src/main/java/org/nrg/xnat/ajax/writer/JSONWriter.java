package org.nrg.xnat.ajax.writer;

/**
 * Facade for JSONWriter. The real implementation lives in apps/web.
 * This facade exists so xnat-data-models can compile against the type.
 * At runtime, apps/web's WEB-INF/classes version takes precedence.
 */
public class JSONWriter {
}
